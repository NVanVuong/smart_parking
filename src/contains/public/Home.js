import React from 'react';
import { Header, SearchUser } from '~/components';

function Home() {
    return (
        <div>
            <Header />
            <SearchUser />
        </div>
    );
}

export default Home;
